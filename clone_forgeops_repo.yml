- name: "git clone forgeops if not already present"
  command: "git clone {{ forgeops_repo_url }}"
  args:
      chdir: "{{ dev_dir }}"
      creates: "{{ forgeops_dir }}"
  register: "output"

- debug: var=output.stdout_lines